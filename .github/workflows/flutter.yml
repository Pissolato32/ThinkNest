name: Flutter CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: flutter-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.7'
          cache: true

      - name: Generate Flutter platform scaffolding
        run: flutter create . --platforms=android,web --no-pub

      - name: Get dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Apply Dart formatting temporarily
        run: dart format lib test

      - name: Upload formatted source for diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: formatted-source-${{ github.run_id }}
          path: |
            lib/
            test/
          retention-days: 7

      - name: Analyze
        run: |
          set +e
          flutter analyze 2>&1 | tee flutter-analyze.log
          status=${PIPESTATUS[0]}
          exit $status

      - name: Test
        run: flutter test

      - name: Upload CI diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-ci-diagnostics-${{ github.run_id }}
          path: |
            flutter-analyze.log
            dart-format.log
          if-no-files-found: ignore
          retention-days: 7
